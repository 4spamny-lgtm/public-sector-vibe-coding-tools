# Contributing

여러 사람이 안전하게 고치고 합치기 위한 최소 규칙입니다.

1. `main`에서 바로 작업하지 않고 `feature/기능명` 브랜치를 만듭니다.
2. 한 Pull Request에는 가능한 한 한 가지 기능만 담습니다.
3. 실제 개인정보, 내부 문서, 실사용 CSV/JSON/XLSX/PDF/HWPX를 커밋하지 않습니다.
4. 데이터가 필요한 기능은 `sample/`의 가상 데이터를 사용합니다.
5. 화면이 바뀌면 `screenshots/main.png`도 갱신합니다.
6. 로컬 실행과 GitHub Pages 실행을 모두 확인한 뒤 PR을 올립니다.

## 커밋 예시
- `feat: 위원회 회의 검색 필터 추가`
- `fix: JSON 복원 시 빈 값 오류 수정`
- `docs: 청소년과 포털 사용법 보완`

## 코드 구조
현재 일부 프로젝트는 단일 HTML 파일입니다. 협업이 커지면 HTML/CSS/JS를 분리하되, 사용자에게는 계속 간단한 실행 경험을 제공하는 방향을 권장합니다.
